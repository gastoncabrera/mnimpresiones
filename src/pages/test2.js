import React, { useState } from 'react';

export default function test2() {
  const [test, settest] = useState(second);

  return (
    <div>
      <input type="text" />
      <button>add</button>
    </div>
  );
}
